let rasenDestroyerArray = [
    new RasenDestroyer(randomNumber(0, map), randomNumber(0, map)),
    new RasenDestroyer(randomNumber(0, map), randomNumber(0, map)),
    new RasenDestroyer(randomNumber(0, map), randomNumber(0, map)),
    new RasenDestroyer(randomNumber(0, map), randomNumber(0, map)),
    new RasenDestroyer(randomNumber(0, map), randomNumber(0, map))
  ];
  
  let waterArray = [
    new Water(randomNumber(0, map), randomNumber(0, map)),
    new Water(randomNumber(0, map), randomNumber(0, map)),
    new Water(randomNumber(0, map), randomNumber(0, map)),
    new Water(randomNumber(0, map), randomNumber(0, map))
  ];
  
  let grassArray = [
    new Grass(randomNumber(0, map), randomNumber(0, map)),
    new Grass(randomNumber(0, map), randomNumber(0, map))
  ];